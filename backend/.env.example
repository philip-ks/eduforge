# Copy to .env and adjust values
PORT=4000
DATABASE_URL="file:./dev.db"

# JWT secret for auth tokens
JWT_SECRET="change_me_super_secret"

# CORS: your frontend origin (or leave * for dev)
CORS_ORIGIN="*"